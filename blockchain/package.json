{
  "name": "govichain-blockchain",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "restart": "powershell -ExecutionPolicy Bypass -File restart-chain.ps1"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.24.3"
  }
}
